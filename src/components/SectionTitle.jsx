import React from 'react'

const SectionTitle = ({title}) => {
  return (
    <h1 className='text-light-text dark:text-dark-text text-2xl font-semibold mb-2'>{title}</h1>
  )
}

export default SectionTitle
